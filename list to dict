ls = [1, 3, 5, 7, 8, 8, 5]
def foo(ls):
    counts_dict = {}
    for i in ls:
        if i in counts_dict:
            counts_dict[i] += 1
        else:
            counts_dict[i] = 1
    return counts_dict
print(foo(ls))


